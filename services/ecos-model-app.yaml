services:
  app:
    logging:
      options:
        max-size: "10m"
        max-file: "5"
    hostname: ecos-model-app
    restart: always
    stop_grace_period: 1m
    image: nexus.citeck.ru/ecos-model:2.37.3
    ports:
      - 18025:18025/tcp
    environment:
      - SERVER_PORT=18025
    env_file:
      - ./environments/ecos-model-app.env
    healthcheck:
      test:
        - CMD
        - bash
        - -c
        - 'exec 3<>/dev/tcp/localhost/18025; printf "GET /management/health HTTP/1.0\r\nHost: x\r\n\r\n" >&3; grep -q ''"status":"UP"'' <&3'
      interval: 20s
      timeout: 20s
      retries: 36
    networks:
      - app_network
