services:
  app:
    logging:
      options:
        max-size: "10m"
        max-file: "5"
    hostname: ecos-process-app
    restart: always
    stop_grace_period: 1m
    image: nexus.citeck.ru/ecos-process:2.30.7
    ports:
      - 18027:18027/tcp
    environment:
      - SERVER_PORT=18027
    env_file:
      - ./environments/ecos-process-app.env
    healthcheck:
      test:
        - CMD
        - bash
        - -c
        - 'exec 3<>/dev/tcp/localhost/18027; printf "GET /management/health HTTP/1.0\r\nHost: x\r\n\r\n" >&3; grep -q ''"status":"UP"'' <&3'
      interval: 20s
      timeout: 20s
      retries: 36
    networks:
      - app_network
